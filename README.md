# 🏃 DASH RUN! - 실시간 스프린트 게임

Vue 3 + TypeScript + Firebase Realtime Database를 사용한 실시간 멀티플레이어 달리기 게임입니다.

## 🎮 게임 방법

1. 닉네임을 입력하고 로그인
2. 레이스 대기실에서 참가 버튼 클릭
3. 관리자가 레이스를 시작하면 카운트다운 시작
4. **버튼을 빠르게 연타**해서 캐릭터를 달리게 하세요!
5. 결승선에 먼저 도착하면 승리!

## 🚀 프로젝트 설정

### 1. 패키지 설치

```sh
npm install
```

### 2. Firebase 설정

1. [Firebase Console](https://console.firebase.google.com/)에서 프로젝트 생성
2. Realtime Database 생성 및 활성화
3. `src/config/firebase.ts`에 Firebase 설정 입력

### 3. 개발 서버 실행

```sh
npm run dev
```

### 빌드

```sh
npm run build
```

### 린트

```sh
npm run lint
```

## 📱 주요 기능

- **실시간 레이스**: Firebase Realtime Database를 통한 실시간 멀티플레이어 경쟁
- **캔버스 애니메이션**: 부드러운 캐릭터 달리기 애니메이션
- **관리자 기능**: 레이스 시작/초기화/참가자 관리
- **카운트다운**: 레이스 시작 전 3초 카운트다운
- **순위 시스템**: 실시간 순위 표시 및 완주 시간 기록

## 🛠 기술 스택

- **Frontend**: Vue 3, TypeScript, Vite
- **State Management**: Pinia
- **Backend**: Firebase Realtime Database
- **Animation**: HTML5 Canvas
- **Styling**: CSS3 with modern gradients and animations

## 📂 프로젝트 구조

```
├── src/
│   ├── components/
│   │   ├── LoginPage.vue        # 로그인/닉네임 입력
│   │   ├── SprintDisplay.vue    # 스프린트 시청 화면 (캔버스 애니메이션)
│   │   ├── SprintRunner.vue     # 참가자용 페이지 (버튼 연타)
│   │   └── SprintAdmin.vue      # 관리자 페이지 (스프린트 관리)
│   ├── config/
│   │   └── firebase.ts          # Firebase 설정
│   ├── router/
│   │   └── index.ts             # 라우터 설정
│   ├── types/
│   │   └── index.ts             # 공통 타입 정의
│   ├── App.vue                  # 메인 앱 컴포넌트
│   └── main.ts                  # 앱 엔트리 포인트
├── stores/
│   └── userStore.ts             # 사용자 상태 관리 (Pinia)
└── public/
    └── sprite-animation4.png    # 캐릭터 스프라이트
```

### 주요 컴포넌트

| 컴포넌트 | 설명 |
|---------|------|
| `LoginPage.vue` | 닉네임 입력 및 로그인 처리 |
| `SprintDisplay.vue` | 캔버스 기반 실시간 스프린트 시청 화면 |
| `SprintRunner.vue` | 참가자가 버튼을 연타하며 달리는 게임 화면 |
| `SprintAdmin.vue` | 스프린트 시작, 초기화, 참가자 관리 |

### 레이스 상태

```typescript
type RaceStatus = 'waiting' | 'countdown' | 'started' | 'finished';
```

- `waiting`: 참가자 대기 중
- `countdown`: 3초 카운트다운 중
- `started`: 레이스 진행 중
- `finished`: 레이스 종료

## 🔧 개발 가이드

자세한 로컬 개발 가이드는 [로컬_개발_가이드.md](./로컬_개발_가이드.md)를 참고하세요.

## 📝 라이선스

MIT
